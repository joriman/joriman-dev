<section class="flex justify-center items-center w-full mt-14 bg-purple">
    <div class="w-full">
        <div>
            <h1 class="skills-header">My Skills</h1>
            <div class="skill-cards">
                <div class="card">
                    <h3 class="heading">Web Applications</h3>
                    <p>HTML, CSS, Sass</p>
                    <p>Javascript</p>
                    <p>React, Next.js</p>
                    <p>Svelte</p>
                </div>
                <div class="card">
                    <h3 class="heading">Software</h3>
                    <p>.NET</p>
                    <p>Java</p>
                    <p>Python</p>
                    <p>C#</p>
                    <p>Electron</p>
                </div>
                <div class="card">
                    <h3 class="heading">Mobile</h3>
                    <p>Swift</p>
                    <p>Kotlin</p>
                    <p>React-Native</p>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
        .skill-cards {
            display: flex;
            justify-content: center;
            align-items: center;
            border-radius: 10px;
            margin-top: 4em;
            margin-bottom: 5rem;
        }

        .card {
            position: relative;
            width: 200px;
            height: 264px;
            background-color: #333;
            display: flex;
            align-items: center;
            flex-direction: column;
            padding: 12px;
            gap: 12px;
            border-radius: 8px;
            cursor: pointer;
            margin: 2rem;
        }

        .card::before {
            content: '';
            position: absolute;
            inset: 0;
            left: -5px;
            margin: auto;
            width: 200px;
            height: 264px;
            border-radius: 10px;
            background: linear-gradient(-45deg, rgb(160, 0, 255) 0%, #40c9ff 100% );
            z-index: -10;
            pointer-events: none;
            transition: all 0.6s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        .card::after {
            content: "";
            z-index: -1;
            position: absolute;
            inset: 0;
            background: linear-gradient(-45deg, #fc00ff 0%, #00dbde 100% );
            transform: translate3d(0, 0, 0) scale(0.95);
            filter: blur(20px);
        }

        .heading {
            color: #fff;
            font-size: 20px;
            text-transform: capitalize;
            font-weight: 700;
        }

        .card p {
            color: #fff;
            font-size: 16px;
        }

        .card:hover::after {
            filter: blur(30px);
        }

        .card:hover::before {
            transform: rotate(-90deg) scaleX(1.34) scaleY(0.77);
        }

        h1 {
            text-align: center;
            padding-top: 3rem;
            font-weight: 700;
            font-size: 2rem;
        }


</style>